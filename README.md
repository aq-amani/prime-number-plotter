# prime_plotter.py
A script to play around with prime numbers.

## 🛠️ Setup/ Preparation
```bash
pipenv install --ignore-pipfile --python 3.8
pipenv shell
```

## Usage examples

## Note
Might need
```bash
sudo apt-get install python3-tk # for GUI backend
sudo apt-get install python3.8-dev # needed to pip install pyaudio
sudo apt-get install portaudio19-dev python-pyaudio
```
